<%= form_for @place, url:{action: "create"} do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<br />
	<%= f.label :phone%>
	<%= f.text_field :phone%>
	<br />
	<%= f.label :info%>
	<%= f.text_field :info%>
	<br />
	<%= f.submit "Add" %>
<% end %>
